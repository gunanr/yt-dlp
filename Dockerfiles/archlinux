# vim: filetype=dockerfile

FROM docker.io/archlinux:latest

RUN pacman-key --init && \
    pacman-key --populate && \
    pacman -Sy --noconfirm \
      archlinux-keyring && \
    pacman -Syu --noconfirm \
      yt-dlp \
      ffmpeg \
      tmux \
      vim 

